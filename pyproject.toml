[project]
name = "slai-tool"
version = "0.1.0"
description = "SenseCore helper scripts for SLAI accounts"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0.3",
    "textual>=8.2.8,<9",
    "tomlkit>=0.15.1",
    "urllib3[socks]>=2.7,<3",
]
